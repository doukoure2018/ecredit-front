export interface PetitCredit {
  id?: number; // Optional ID
  moyenPerson?: string; // Le moyen de déplacement
  bien?: string; // Property or asset
  capital?: number; // Capital amount
  creance?: number; // Outstanding amount
  dette?: number; // Debt amount
  statutActivite?: string; // Activity status
  experience?: string; // Experience level
  lieuxAct?: string; // Activity location
  personEmp?: string; // Person employed
  lien?: string; // Link or connection
  nombre?: string; // Number or quantity
  referenceCredit?: string; // Credit reference
  cumulCredit?: number; // Cumulative credit amount
  nbreCredit?: number; // Number of credits
}
